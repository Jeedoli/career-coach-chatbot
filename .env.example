# =============================================================================
# AI Challenge - 커리어 코치 챗봇 API 환경 설정
# =============================================================================

# OpenAI API 설정 (필수)
# https://platform.openai.com/api-keys 에서 발급
OPENAI_API_KEY=sk-proj-your-actual-api-key-here

# Django 기본 설정
SECRET_KEY=django-insecure-change-this-in-production-minimum-50-characters-long
DEBUG=False  # 프로덕션에서는 False로 설정
ALLOWED_HOSTS=your-domain.com,www.your-domain.com,

# 데이터베이스 설정 (기본: SQLite)
DATABASE_URL=sqlite:///db.sqlite3

# CORS 설정 (프론트엔드 허용 도메인)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# =============================================================================
# 프로덕션 배포용 설정 예시 (EC2 + 도메인)
# =============================================================================

# SECRET_KEY=your-super-secret-production-key-minimum-50-characters-long
# DEBUG=False
# ALLOWED_HOSTS=your-domain.com,www.your-domain.com,your-ec2-ip,localhost
# CORS_ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com

# =============================================================================
# 선택적 설정
# =============================================================================

# 로그 레벨 (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# 캐시 설정 (Redis 사용 시)
# REDIS_URL=redis://localhost:6379/0

# 이메일 설정 (에러 알림용)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password